name: Oliver Roick
markdown: kramdown
highlighter: rouge
permalink: /:categories/:year/:title.html
excerpt_separator: "====="
source: src
collections:
  work:
    output: false
  til:
    output: true
    permalink: /:collection/:year/:title.html
plugins:
  - jekyll-sitemap
  - ./_plugins/post_update
defaults:
  - scope:
      path: "files/**/*"
    values:
      sitemap: false
  - scope:
      path: "404.html"
    values:
      sitemap: false
